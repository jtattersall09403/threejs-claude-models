// Material region ids carried on the `aRegion` vertex attribute.
export const REGION = {
  SKIN: 0,      // scaled hide (colour variation is derived from rest position in the shader)
  HORN: 1,      // horns, crown/jaw spikes, claws, teeth
  EYE: 2,       // eyeball
  TUNIC: 3,     // coarse outer tunic
  UNDERSHIRT: 4,
  LEATHER: 5,   // shoulder strap, belt, shoes
  TROUSERS: 6,
  WRAP: 7,      // pale wrist wraps
  SASH: 8,      // braided shoulder strap — lighter than the tunic it lies on
  BELT: 9,
};
